#!/usr/bin/env ruby

if system("which thin")
  system("thin -c #{File.dirname(__FILE__) + '/../'} #{ARGV.join(' ')} start")
else
  require File.dirname(__FILE__) + '/../config/boot'
  require 'commands/server'
end
