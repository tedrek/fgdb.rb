<h1>Editing <%= singular_name %></h1>

<%% form_for(@<%= singular_name %>, :url => {:action => "update", :id => @<%= singular_name %>.id}) do |f| %>
  <%%= f.error_messages %>

  <%%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%%= f.submit "Update" %>
  </p>
<%% end %>

<%%= link_to 'Show', {:action => "show", :id => @<%= singular_name %>.id} %> |
<%%= link_to 'Back', {:action => "index"} %>
