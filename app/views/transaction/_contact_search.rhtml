<% options ||= {} %>
<div class="form-element">
  <%= contact_field("@transaction", "contact",
        :locals => {:options => {
            :select_name => @transaction_type + '[contact_id]'
          }.merge(options)
        }) %>
  <%# render :partial => 'contacts/search',
        :locals => {:options =>
            {:select_name => @transaction_type + '[contact_id]'}.merge(options),
        :contact => @transaction.contact}
  %>
</div>