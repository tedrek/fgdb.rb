page.replace( totals_id(@transaction_type),
              :partial => @transaction_type + '_totals',
              :locals => {"@#{@transaction_type}" => @transaction} )
if @transaction_type == "sale"
  page << "update_all_gizmo_totals();"
end
