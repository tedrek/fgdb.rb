<h1>Editing pricing_type</h1>

<% form_for(@pricing_type, :url => {:action => "update", :id => @pricing_type.id}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', {:action => "show", :id => @pricing_type.id} %> |
<%= link_to 'Back', {:action => "index"} %>
