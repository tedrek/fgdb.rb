<h1>New volunteer_default_event</h1>
  <% @volunteer_event = @volunteer_default_event %>

<%= form_for(@volunteer_default_event, :url => {:action => "create"}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', {:action => "index"} %>
