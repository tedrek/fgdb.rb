<h1>Editing <%= @property_type %></h1>

<% form_tag({:action => "update", :id => @property.id}) do |f| %>
  <p>
    <b>Name</b><br />
    <%= text_field "property", :name %>
  </p>

  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Back', :action=>"index" %>
