buttons_id = submit_choice_buttons_id(params)
@options = { :scaffold_id => params[:scaffold_id], :action => "update", :id => params[:id] }
page.replace buttons_id, :partial => 'submit_choice_buttons'
page.visual_effect :highlight, buttons_id, :duration => 3
page.show buttons_id
