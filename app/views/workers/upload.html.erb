<h2>Uploading picture for <%= @worker.name %></h2>

<% if @error %>
<span class="error">
Error: <%= @error %><br /><br />
</span>
<% end %>

Current image:
<img width="160px;" height="200px" src="/images/workers/<%= @worker ? @worker.id : 0 %>.png" alt="none" />

<br />
<br />
<br />

Upload a .png image (will be displayed using 160x200 pixels):
<% form_tag({:action => "upload", :id => @worker.id}, :multipart => true) do %>
  <%= file_field_tag 'picture' %>
  <%= submit_tag "Upload" %>
<% end %>
