  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <h2>Schedules:</h2>
  <% for sked in Sked.find(:all) %>
    <div>
      <%= check_box_tag "roster[sked_ids][]", sked.id, @roster.skeds.include?(sked) %>
      <%= sked.name %>
    </div>
  <% end %>
