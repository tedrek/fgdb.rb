<h1>Editing pricing_value</h1>

<% form_for(@pricing_value, :url => {:action => "update", :id => @pricing_value.id}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', {:action => "show", :id => @pricing_value.id} %> |
<%= link_to 'Back', {:action => "index"} %>
