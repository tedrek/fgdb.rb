<h1>Editing volunteer_shift</h1>

<%= form_for(@volunteer_shift, :url => {:action => "update", :id => @volunteer_shift.id}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', {:action => "index"} %>
