<h1>New work_order</h1>

<% form_for(@work_order, :url => {:action => "create"}) do |f| %>
  <%# f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', {:action => "index"} %>
<%= javascript_tag "prep_disabled_list(linelist, optional, hook_list);" %>
