<!DOCTYPE html
          PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
          "DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" lang="en">
  <head>
    <title>Free Geek Database:
      <%= (controller.class.name).titleize.sub(/ Controller/, '') %>:
      <%= (controller.action_name).titleize %>
    </title>
    <%= stylesheet_link_tag 'scaffold', 'lightwindow', 'application', :media => 'all' %>
    <%= stylesheet_link_tag 'no_print', :media => 'print' %>
    <%= javascript_include_tag :defaults %>
    <%= javascript_include_tag 'lightwindow' %>
    <%= javascript_include_tag 'boxover' %>
    <%= javascript_include_tag 'keybindings' %>

    <%= javascript_tag "function good_cashier_code(code) {
" + remote_function(
          :url => {:controller => "contacts", :action => "check_cashier_code"},
                          :loading => "Element.show('#{loading_indicator_id("cashier_loading")}');",
                          :with => "{cashier_code: code}") + "
}" %>

    <%= dhtml_calendar_includes %>
    <% if defined?(@gizmo_context) %>
      <%= render :partial => 'sidebar_links/line_item_javascript', :locals => {:@gizmo_context => @gizmo_context} %>
    <% end %>
  </head>
  <body>

    <div id="logo">
      <a href="/"><img src="/images/freegeeklogo.png" alt="freegeeklogo.png" class="image-link"/></a>
    </div>

    <% if !(session[:unauthorized_error] && !logged_in?) %>
      <%= render :partial => "sessions/you_are_here" %>
    <% else %>
      <% session[:unauthorized_error] = false %> <%# rails renders the layouts last %>
      <% session[:unauthorized_controller] = nil %>
      <% session[:unauthorized_action] = nil %>
      <%= javascript_tag "var pixelsize='0px';" %>
    <% end %>

    <%= render :partial => 'sidebar_links/sidebar' %>

    <div id="<%= content_id %>">

      <%= javascript_tag "if(pixelsize) {$('content').style.top=pixelsize;}" %>
