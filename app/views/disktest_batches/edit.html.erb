<h1>Editing disktest batch</h1>

<% form_for(@disktest_batch, :url => {:action => "update", :id => @disktest_batch.id}) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', {:action => "show", :id => @disktest_batch.id} %> |
<%= link_to 'Back', {:action => "index"} %>
