<h1>Editing system_pricing</h1>

<%= form_for(@system_pricing, :url => {:action => "update", :id => @system_pricing.id}) do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', {:action => "show", :id => @system_pricing.id} %> |
<%= link_to 'Back', {:action => "index"} %>
