<% for column in StandardShift.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @standard_shift.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @standard_shift %> |
<%= link_to 'Back', :action => 'list' %>
