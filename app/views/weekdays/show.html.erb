<% for column in Weekday.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @weekday.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @weekday %> |
<%= link_to 'Back', :action => 'list' %>
