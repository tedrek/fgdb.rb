<h1>Editing role</h1>

<% form_tag({:action => "update", :id => @role.id}) do |f| %>
  <p>
    <b>Name</b><br />
    <%= text_field "role", :name %>
  </p>

  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Back', :action=>"index" %>
