<div id="hours_report" class="hours_report">
<% if contact %>
  <%= contact.display_name %> (<%= contact.id %>) has worked:<br />
  <span class="hours"><%= contact.hours_actual(true) %></span> hours in the last 90 days<br />
  <span class="hours"><%= contact.hours_actual %></span> hours ever<br />
  <span class="hours"><%= contact.hours_effective %></span> hours toward adopting a computer<br />
<% end %>
</div>