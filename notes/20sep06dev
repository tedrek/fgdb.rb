
### roadmap for 21 sep

. remove column 'user_id' from contacts table and regenerate or
adjust appropriate files

#===========================================================
# 20 sep

### roadmap for 20 sep
. port over model relationship statements from wobble

. create sample data using screens

. auto-populate fixtures from data entered by hand

. replace tabs with spaces in all auto-generated files
  $ util/find_files_w_tabs         -> creates /tmp/tabfiles
  $ cp /tmp/tabfiles util/tabfiles -> save for reference (in svn)
  $ vi util/tabfiles               -> sanity check before using
  $ perl -pi -e 's/\t/  /g' `cat util/tabfiles`

. add dropdown box code from wobble to some view files to allow
creation and editing where "has" relationship exists

  - contact_methods needs contact_method_types dropdown
    $ vi app/views/contact_methods/_form.rhtml

+ <div class="form-element">
+ 
+ <label for="contact_method_type_id">Contact method type</label><br/>
+ <select id="contact_contact_method_type_id" name="contact_method[contact_method_type_id]">
+   <%= options_from_collection_for_select ContactMethodType.find_all, "id", "description", @contact_method.contact_method_type_id %>
+ </select>
+ </div>

  - contacts needs contact_types
  - relationships needs relationship_types

. restart server and create some data using screens

. dump and reload devel database to/from db/devel_data.sql
$ rake db:data:dump
$ rake db:data:load

. dump to fixtures from devel db then reload devel db from fixtures
$ rake db:fixtures:export_all
$ rake db:fixtures:import_all

. figure out why contacts "create new" screen will not load
after adding in dropdown box code 
  - probably to do with the find_all class
  - switched .../views/contacts/_form.rhtml to use
    checkbox-centric code fr analogous wobble html partial
    * still does not load

  - dev log gives error:

  ActionView::TemplateError (You have a nil object when you didn't expect it!
  You might have expected an instance of Array.
  The error occured while evaluating nil.each) on line #70 of
  app/views/contacts/_ form.rhtml:
  70: <% for contact_type in @contact_types %>

  - added this line to app/controllers/contacts_controller.rb#new:
   @contact_types = ContactType.find_all

